services:
  - type: web
    name: finanzas-pro-360
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --preload"
    plan: free
    autoDeploy: true
